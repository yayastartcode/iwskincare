# Database - PostgreSQL (Neon, Supabase, atau local)
DATABASE_URL=postgresql://username:password@host/database?sslmode=require

# Payload Secret - Generate dengan: openssl rand -base64 32
PAYLOAD_SECRET=your-super-secret-key-minimum-32-characters

# Revalidation Secret - Generate dengan: openssl rand -base64 32
# Digunakan untuk memvalidasi request revalidation dari Payload
REVALIDATE_SECRET=your-revalidation-secret-key

# Site URL (untuk SEO, social share, dll)
NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# Node Environment
NODE_ENV=production
